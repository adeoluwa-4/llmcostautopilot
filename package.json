{
  "name": "llm-cost-autopilot",
  "version": "0.1.0",
  "private": true,
  "description": "A cost-aware, quality-constrained routing layer for LLM providers.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
